//Напишите функцию, которая принимает массив чисел и возвращает новый массив, где каждый элемент увеличен на один.

const arr = [1, 2, 3, 4, 5, 6, 7]

const newArr = arr.map((el) => el + 1)

console.log(newArr);